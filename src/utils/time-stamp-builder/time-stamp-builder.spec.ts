import { timeStampBuilder } from "./index";

describe("utils/Time Stamper Builder", () => {
  beforeEach(() => {});

  afterEach(() => {});

  it("should be a function", () => {
    expect(typeof timeStampBuilder).toEqual("function");
  });

  it("should return the date in the correct format", () => {});
});
