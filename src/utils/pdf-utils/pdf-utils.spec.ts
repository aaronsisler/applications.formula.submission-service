import { padRight } from "./index";

describe("Utils/PDF Utils", () => {
  describe("padRight", () => {
    it("should be a function", () => {
      expect(typeof padRight).toEqual("function");
    });
  });
});
