import { orderMultiSubSection } from "./index";

describe("utils/Order Sub Section Filter", () => {
  beforeEach(() => {});

  afterEach(() => {});

  it("should be a function", () => {
    expect(typeof orderMultiSubSection).toEqual("function");
  });
});
