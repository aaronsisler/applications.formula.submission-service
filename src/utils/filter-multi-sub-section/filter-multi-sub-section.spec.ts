import { multiSubSectionFilter } from "./index";

describe("utils/Multi Sub Section Filter", () => {
  beforeEach(() => {});

  afterEach(() => {});

  it("should be a function", () => {
    expect(typeof multiSubSectionFilter).toEqual("function");
  });
});
